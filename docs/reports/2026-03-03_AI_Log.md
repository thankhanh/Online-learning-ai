# 📅 Báo Cáo Hàng Ngày - AI Engineer (Thành viên 3)
**Ngày**: 03/03/2026
**Người thực hiện**: AI Engineer

## ✅ Công việc đã hoàn thành

1.  **Đồng bộ & Giải quyết xung đột Git**:
    - Đã kéo code mới nhất từ nhánh `dev` về nhánh `AI`.
    - Giải quyết xung đột tại `frontend/package-lock.json` (chọn bản của `dev` để đồng bộ dependency).
    - Giải quyết xung đột tại `backend/.env` (giữ cấu hình `OLLAMA_URL=http://localhost:11434` cho môi trường local).

2.  **Triển khai RAG Pipeline (`backend/src/services/ai/ragPipeline.js`)**:
    - Sử dụng `pdf-parse` để trích xuất văn bản từ tài liệu PDF.
    - Sử dụng `RecursiveCharacterTextSplitter` từ LangChain để chia nhỏ văn bản (chunking).
    - Khởi tạo `MemoryVectorStore` với `OllamaEmbeddings` (`nomic-embed-text`) để lưu trữ và tìm kiếm vector nội dung.
    - Hoàn thành hàm `ingestDocument` và `retrieveContext`.

3.  **Tích hợp AI Service (`backend/src/services/ai/aiService.js`)**:
    - Kết nối với mô hình `qwen2.5:1.5b` qua Ollama.
    - Xây dựng logic hỏi đáp có ngữ cảnh (Context-aware Q&A): Lấy dữ liệu liên quan từ RAG Pipeline trước khi gửi prompt tới AI.

4.  **Phát triển API AI**:
    - **Controller (`backend/src/controllers/aiController.js`)**: Viết logic xử lý cho endpoint `/ask` (hỏi đáp) và `/ingest` (nạp tài liệu).
    - **Routes (`backend/src/routes/aiRoutes.js`)**: Định nghĩa các route cho AI.
    - **App Integration (`backend/index.js`)**: Đăng ký AI routes vào server chính tại `/api/ai`.

## 🚧 Công việc đang thực hiện / Bước tiếp theo

1.  **Cải thiện xử lý File**:
    - Tích hợp `multer` để hỗ trợ upload file PDF trực tiếp từ client (hiện tại đường dẫn đang được giả định hoặc truyền thủ công).
2.  **Tối ưu hóa Vector Database**:
    - Nghiên cứu sử dụng MongoDB Atlas Vector Search để lưu trữ vector lâu dài thay vì dùng bộ nhớ RAM (MemoryVectorStore).
3.  **Giao diện người dùng**:
    - Hỗ trợ Frontend Engineer kết nối Sidebar Chat với API AI mới tạo.

## 📝 Ghi chú
- Tất cả code đã được kiểm tra cú pháp và đẩy lên nhánh `AI` (`origin/AI`).
- Yêu cầu môi trường local phải cài đặt Ollama và chạy model `qwen2.5:1.5b` và `nomic-embed-text`.
