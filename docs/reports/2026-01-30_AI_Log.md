# 📅 Báo Cáo Hàng Ngày - AI Engineer (Thành viên 3)
**Ngày**: 30/01/2026
**Người thực hiện**: AI Engineer

## ✅ Công việc đã hoàn thành (Môi trường & Thiết lập)

1.  **Khởi tạo dự án & Đồng bộ Git**:
    - Đã kiểm tra nhánh `feature/AI` (tên local là `AI`).
    - Xác nhận trạng thái nhánh: Đang nhanh hơn `origin/AI` 7 commit.
    - **Đã đồng bộ**: Commit file `frontend/package-lock.json` để khớp với dependency của nhánh `dev`.

2.  **Cấu hình môi trường**:
    - Kiểm tra `docker-compose.yml` (gặp lỗi build với Docker Daemon local).
    - Chuyển sang **Chế độ phát triển Local** (npm run dev).
    - **Backend**:
        - Kết nối thành công tới **MongoDB Atlas** (Cloud).
        - Cấu hình `OLLAMA_URL` thành `http://localhost:11434` để truy cập AI local.
    - **Frontend**:
        - Đã chạy thành công trên cổng `5173`.

3.  **Lập kế hoạch**:
    - Phân tích `PROJECT_PLAN_10_WEEKS.md` và `TEAM_TASKS.md`.
    - Tạo `implementation_plan.md` cho hệ thống **RAG Pipeline** (Xử lý tài liệu & Truy xuất thông tin).

## 🚧 Công việc đang thực hiện / Bước tiếp theo

1.  **Triển khai RAG Pipeline** (`backend/src/services/ai/ragPipeline.js`):
    - Viết hàm `ingestDocument` (Đọc PDF -> Lưu vào Vector Store).
    - Viết hàm `retrieveContext`.
2.  **Tích hợp AI Service** (`backend/src/services/ai/aiService.js`):
    - Kết nối RAG pipeline với mô hình Qwen qua Ollama.

## 📝 Ghi chú gửi Leader
- Build Docker gặp lỗi "500 Internal Server Error" trên máy này nên em đang chạy trực tiếp (local).
- Đã cập nhật `package-lock.json` ở Frontend để đảm bảo tính nhất quán cho cả đội.